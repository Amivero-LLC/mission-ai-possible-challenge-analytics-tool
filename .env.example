# Backend service
# Port exposed by Docker compose / local uvicorn. Change if 8000 conflicts with other apps.
BACKEND_PORT=8000
# Optional absolute/relative path to a specific OpenWebUI export JSON. Leave blank to auto-discover latest file in data/.
MISSION_DATA_FILE=
# Optional path to custom user name mapping (user_id -> display name). Defaults to data/user_names.json when empty.
MISSION_USER_NAMES_FILE=
# When set, backend fetches chats from the live OpenWebUI instance instead of local exports.
OPEN_WEBUI_HOSTNAME=replace-with-hostname
# API key paired with OPEN_WEBUI_HOSTNAME. Provide a bearer token or leave empty to disable remote fetch.
OPEN_WEBUI_API_KEY=replace-with-api-key

# Frontend service
# Port exposed by docker compose / next dev server. Change if 3000 conflicts locally.
FRONTEND_PORT=3000
# Browser-visible URL for the FastAPI backend. Must be reachable from the user's machine.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# Internal API base used by Next.js server-side fetches (e.g., via docker network).
API_BASE_URL=http://backend:8000
